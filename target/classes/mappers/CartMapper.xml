<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jafa.repository.CartRepository">
  
  	<!-- 카트 목록 -->
	<select id="getCart" resultType="cartVO">
		SELECT distinct
		  c.mno, 
		  p.PRODUCT_BNO, p.PRODUCT_NAME, p.PRODUCT_PRICE
		FROM 
		  cart_tbl c
		JOIN member_tbl m ON c.mno = m.mno
		JOIN product_tbl p ON c.product_bno = p.PRODUCT_BNO
		where c.mno=#{mno}
	</select>
	
	<select id="getTotalCount" resultType="int">
		select sum(product_count) as product_count  
		from cart_tbl where mno=#{mno} and product_bno=#{product_Bno} 
	</select>

	<!-- 카트 상세 목록 -->
	<select id="checkCart" resultType="cartVO">
		select * from cart_tbl 
		where memberId = #{memberId} and product_bno = #{product_bno}
	</select>
  
	<!-- 카트 추가 -->
	<insert id="addCart">
		insert into cart_tbl(memberId, product_bno, product_Count)
		values(#{memberId}, #{product_bno}, #{product_Count}) 
	</insert>
	
	<!-- 카트 수량 수정 -->
	<update id="modifyCount">
		update cart_tbl set product_Count=#{product_Count} where cartId = #{cartId}
	</update>
	
	<!-- 카트 삭제 -->
	<delete id="deleteCart">
		delete from cart_tbl where cartId = #{cartId}
	</delete>
	
</mapper>